#include "main.h"
/**
 * _abs - function that commpute the absolute value of a function
 * @c: is the int used for arrgument function
 * Return: 0
 */
int _abs(int c)
{
	if (c > 0 || c == 0)
	{
		return (c);
	}
	else

	return (c * -1);
}
