#include "main.h"
/**
 * _isdigit - function to cheak for digits
 * @c: is the  int used for argument
 * Return: Always 0
 */
int _isdigit(int c)
{
for (c = 0; c <= 9; c++)

if (c == 0)
{
return (0);
}
else
return (1);
}

